{
  "name": "foxex",
  "version": "1.0.0",
  "description": "Combined Bittrex and Poloniex order book. Note:Root package.json manages heroku deployments, see client and server package.jsons for dev scripts",
  "scripts": {
    "start": "node server/build/index.js",
    "build": "npm install --prefix server; npm run build --prefix server; npm install --prefix client; npm run build --prefix client"
  },
  "engines": {
    "node": "14.15.0"
  }
}
